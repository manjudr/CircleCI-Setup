# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1
jobs:
  build-and-test:
    docker:
      - image: cimg/openjdk:11.0
    steps:
      - add_ssh_keys:
          fingerprints:
            - "b8:cf:76:4f:a2:8b:2d:12:fa:27:3a:2a:16:44:e6:a8"
      - checkout
      - run:
          name: Build
          command: mvn -B -DskipTests clean package
      - run:
          name: Test
          command: mvn test
      - run:
          name: Deploy Over SSH
          command: |
            pwd
            ls
            scp -r /home/circleci/project/target/HelloWorld-1.0-SNAPSHOT.jar manjunathdavanam@192.168.1.2:/Users/manjunathdavanam/test
workflows:
  sample:
    jobs:
      - build-and-test